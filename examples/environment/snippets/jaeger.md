+++
description = "Use Jaeger when it is installed on this host"
requires = ["jaeger"]
+++
## Jaeger workflows

Use Jaeger for durable cross-harness workflows. Validate authored workflow files
before launching them, and preserve the returned run ID for later inspection.
