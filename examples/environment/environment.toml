version = 1
name = "example"

[providers.codex]
instructions = ["snippets/jaeger.md"]
# Skills are copied into the provider's native directory. Plugins are installed
# through the provider's native plugin manager.
# skills = ["skills/example-skill"]
# plugins = ["github@openai-curated"]

[providers.claude]
instructions = ["snippets/jaeger.md"]

[providers.pi]
instructions = ["snippets/jaeger.md"]
# Pi packages use Pi-native sources and commands, not plugin@marketplace.
# skills = ["skills/example-skill"]
# packages = ["npm:@acme/pi-tools@1.2.3"]
